# LLM Usage Log

## Запись №1
- **Дата:** 04.03.2026
- **Лабораторная/шаг:** Неделя 2. HTTP/API: первый Extract + raw JSON.
- **Что спросил(а):** - Разбор задания по варианту 14 (World Bank API).
    - Написание Python-скрипта для извлечения данных в рамках существующей структуры пакета `sem2_de`.
    - Помощь в заполнении шаблонов Implementation Plan, Data Contract и этого лога.
- **Что получил(а):** - Готовый код `extract.py` с использованием библиотеки `requests`.
    - Файл конфигурации `variant_14.yml`.
    - Полностью заполненные Markdown-документы по стандартам проекта.
- **Что применил(а) в коде:** - Логику формирования URL для World Bank API.
    - Обработку таймаутов и кодов ответа (raise_for_status).
    - Структуру сохранения данных в `data/raw/` с разбивкой по датам.
- **Как проверил(а) результат:** - Запустил команду `python -m sem2_de.extract` (или через cli).
    - Проверил наличие сгенерированного JSON-файла в папке `data/raw/`.
- **Что оказалось ошибочным/опасным:** - Первоначальная идея запускать скрипт как отдельный файл была скорректирована с учетом того, что проект является модулем (пакетом Python).

## Запись №2 | Неделя 3
Что спросил: Помощь с Неделей 3 (Pandas) после того, как сломался ноутбук и в сырых JSON обнаружилась ошибка API.
Что получил: Код скрипта broken_pandas_read.py и структуру нового Jupyter Notebook для загрузки, нормализации через pd.json_normalize и сохранения очищенных данных в CSV.

## Запись №3 | Неделя 4
- **Дата:** 27.03.2026
- **Лабораторная:** Transform & Mart.
- **Что спросил:** Исправление путей под диск D, обработку папки `usa_co2_emissions` и расчет KPI (темп роста).
- **Что получил:** Скрипт `transform_to_mart.py` с защитой от ошибок в кодировке CSV и динамическим определением колонок справочника.
- **Что применил:** Логику `shift()` для расчетов Year-over-Year и автоматическое определение корня проекта через `os.path.dirname`.
